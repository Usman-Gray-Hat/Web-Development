CREATE DATABASE FORGET_PASSWORD;
USE FORGET_PASSWORD;
-- USERS --
CREATE TABLE USERS
(
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50),
    email VARCHAR(50) UNIQUE,
    password VARCHAR(100)
);
-- TOKENS --
CREATE TABLE TOKENS
(
    id INT PRIMARY KEY AUTO_INCREMENT,
    token VARCHAR(300),
    status INT DEFAULT 1
);
-- INSERTNG DATA INTO USERS TABLE --
INSERT INTO USERS (name,email,password) VALUES 
('Usman Hameed','usmanhameed1790@gmail.com',MD5('a'));
SELECT * FROM USERS;
SELECT * FROM TOKENS;